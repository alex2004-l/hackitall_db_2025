rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /scores/{document} {
      // Oricine poate citi scorurile
      allow read: if true;
      // Doar userii logați pot adăuga scoruri noi
      allow create: if request.auth != null;
    }
  }
}